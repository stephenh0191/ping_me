 
 <%= link_to "Chatroom", root_path, class: "item"  %>
<%= link_to "Users", users_path, class: "item"%>
<%= link_to "Messages",root_path, class: "item"%>


<div class="right menu">
   <div class="ui dropdown item">
      Account <i class="dropdown icon"></i>
    
      <div class="menu">
        <% if logged_in? %>
      <%= link_to "View your Account", user_path(current_user), class: "item"%>
      <%= link_to "Edit your Account", edit_user_path(current_user), class: "item"%>
      <%= link_to "Delete profile", user_path(current_user), 
          class: "item text-danger", method: :delete, data: { confirm: "Are you sure?" } %> 

           <%= link_to "Log out", logout_path , method: :delete, class: "item" %>

        <%else %>

        <%= link_to "Log in", login_path,  class: "item"%> 
        <%end%>
       </div>
   </div>

    <% if !logged_in? %>
   <div class="item">
     <%= link_to "Log in", login_path,  class: "ui primary button"%> 
   </div>
   <%end%>
</div>


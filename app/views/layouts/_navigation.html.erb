 
<%= link_to "Home", login_path, class: "item"  %>
 <% if logged_in? %>
<%= link_to "Users", users_path, class: "item"%>
<%= link_to "Messages",root_path, class: "item"%>
<%end%>

<div class="right menu">
   <div class="ui dropdown item">
      Account <i class="dropdown icon"></i>
    
      <div class="menu">
        <% if logged_in? %>
      <%= link_to "View your Account", user_path(current_user), class: "ui inverted primary button"%>
      <%= link_to "Edit your Account", edit_user_path(current_user), class: "ui inverted green button"%>

           <%= link_to "Log out", logout_path , method: :delete, class: "ui  item " %>

        <%else %>
        <%= link_to "Log in", login_path,  class: "ui inverted secondary button"%> 
        <%end%>
       </div>
   </div>

    <% if !logged_in? %>
   <div class="item">
     <%= link_to "Log in", login_path,  class: "ui primary button"%> 
   </div>
   <%end%>
</div>

